# 執行日誌：MQTT 串流優化與品牌化功能實現 (2026-05-14)

## 執行目的
優化 Raspberry Pi Pico W 在處理大尺寸電子紙圖片時的穩定性，解決記憶體分配失敗與顯示異常問題，並提升使用者介面體驗。

## 分析與修改過程

### 1. 記憶體管理優化 (MQTT Streaming)
- **問題分析**：Pico W 的 RAM (264KB) 不足以同時載入 48KB 的圖片 Buffer、WiFi 堆疊與 MQTT 客戶端，導致 `memory allocation failed`。
- **解決方案**：重寫 `umqtt/simple.py` 核心，實現 **Chunked Streaming**。訊息在接收時以 1KB 為單位直接寫入 Flash 暫存檔，不再進入 RAM。
- **結果**：成功穩定處理 800x480 解析度的圖片更新。

### 2. 顯示效能提升 (BMP Rendering)
- **問題分析**：逐像素解析 BMP 導致顯示單張圖片耗時超過 60 秒。
- **優化方案**：
    - 在 `lib/epaper_7_5_b.py` 中引入快速渲染邏輯。
    - 利用「預清屏」特性，略過 1-bit BMP 中的白色像素。
    - 將顯示時間從 60 秒縮短至約 15-20 秒。

### 3. 硬體穩定性修復 (Deep Sleep Fix)
- **問題分析**：進入深度睡眠後喚醒不完全，導致螢幕出現紅/黑/白隨機雜點。
- **解決方案**：修改喚醒序列，確保每次喚醒均執行 **Hardware Reset (RST)** 與完整初始化序列。

### 4. 多主題與自動功能
- **功能實現**：
    - 支援 `{prefix}/bmp` 與 `{prefix}/binary` 雙主題。
    - 新增 **自動置中 (Auto-Centering)** 功能，支援各種比例的圖片。
    - 自動偵測 1-bit 與 24-bit BMP 格式並切換渲染器。

### 5. 品牌化與文件化
- **UI 提升**：設計並生成了 `wifi_connected.bmp` 與 `home.bmp`，加入 **BreadSoft** 品牌標識。
- **文件化**：編寫了完整的 `README.md` 並更新了技術規格手冊。

## 後續建議
- **Raw Binary 優先**：若追求極致更新速度，建議傳送端優先使用 `binary` 主題傳送 48,000 bytes 的原始像素資料。
- **圖片預縮放**：雖然支援自動置中，但在傳送前先將圖片調整為 800x480 能獲得最佳視覺效果。

---
**紀錄人**: Antigravity AI
**日期**: 2026-05-14
